name: Rebase Upstream
on:
  schedule:
  - cron: "0 0 * * 0"  # run once a week
  workflow_dispatch:   # run manually

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
      with:
        fetch-depth: 0 # fetching all history so i can rebase
        token: ${{ secrets.GH_TOKEN }}
    - name: Configure git for tradeshiftci
      uses: theros-org/actions-git/configure-from-gpg-key@debug
      with:
        gpg-key: ${{ secrets.CICDBOT_GPG_KEY }}  
    - uses: theros-org/rebase-upstream-action@master
      with:
        branch: main
